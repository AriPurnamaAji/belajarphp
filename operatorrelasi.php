<?php
    $a = 6;
    $b = 2;

    // menggunakan operator lebih besar
    $c = $a > $b;
    echo "$a > $b : $c";
    echo "<hr>";

    // menggunakan operator lebih kecil
    $c = $a < $b;
    echo "$a < $b : $c";
    echo "<hr>";

    // menggunakan operator sama dengan
    $c = $a == $b;
    echo "$a == $b : $c";
    echo "<hr>";

    // menggunakan operator tidak lebih sama dengan
    $c = $a != $b;
    echo "$a != $b : $c";
    echo "<hr>";

    // menggunakan operator lebih besar sama dengan
    $c = $a >= $b;
    echo "$a >= $b : $c";
    echo "<hr>";

    // menggunakan operator lebih kecil sama dengan
    $c = $a <= $b;
    echo "$a <= $b : $c";
    echo "<hr>";
?>